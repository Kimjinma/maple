#  메이플스토리 장비 추천 시뮬레이터 (Maple Item Recommender)

> **"가진 자본으로 어떤 아이템부터 바꾸는 게 가장 스펙업이 많이 될까?"**
> 유저들의 실제 고민에서 출발한 **가성비 최적화 장비 추천 백엔드 서비스**입니다.

단순히 좋은 아이템을 나열하는 것을 넘어, **배낭 문제(Knapsack Problem)** 알고리즘을 응용해 (상승 스탯 / 소모 비용) 효율이 가장 높은 최적의 아이템 조합을 계산합니다. 넥슨(Nexon) Open API를 활용해 내 캐릭터의 현재 스펙을 정밀하게 분석합니다.

<br>

## 기술 스택 (Tech Stack)
* **Backend:** Java 17, Spring Boot, Spring Data JPA
* **Database & Cache:** MariaDB, Spring Cache (`@Cacheable`)
* **Infra/DevOps:** AWS EC2, Docker
* **External API:** Nexon Open API

<br>

##  주요 기능 및 고민한 점

### 1. 내 캐릭터 기준 '스탯 가치' 동적 계산
* **데이터 파싱:** 넥슨 API로 전투력, 주스탯, 보스 데미지 등 100여 가지 스펙 데이터를 파싱해 객체화합니다.
* **가치 환산 로직 구현:** 유저마다 '주스탯 1%'나 '공격력 1'이 올려주는 전투력 상승치가 다릅니다. 이를 해결하기 위해 현재 스펙 기준으로 서로 다른 옵션들을 동일한 지표(환산 점수)로 비교할 수 있도록 계산 로직을 직접 구현했습니다.

### 2. 가성비 기반 장비 추천 알고리즘 설계
* **우선순위 선별:** 장착 가능한 장비 데이터 중 가격 대비 점수 상승량이 가장 높은 아이템을 우선 선별합니다.
* **알고리즘 고도화:** 예산 범위 내에서 최적해를 찾다가 중간에 멈춰버리는 현상(Local Maxima)을 방지하기 위해, 알고리즘 후반부에 특정 조건에서 강제로 상위 장비를 탐색해 고점을 뚫는(Forced Upgrade) 예외 로직을 추가하여 추천 정확도를 대폭 높였습니다.

### 3. 외부 API 의존성 극복 및 안정성 확보
* **인메모리 캐싱 도입:** 넥슨 API를 매번 호출하면 속도 제한(Rate Limit)에 걸리거나 페이지 로딩이 느려지는 병목이 있었습니다. Spring의 `@Cacheable`을 이용한 캐싱을 도입하여 불필요한 API 호출을 줄이고 응답 속도를 개선했습니다.
* **전역 예외 처리:** 외부 API 장애 시 서버 전체가 죽지 않도록 `Global Exception Handler`를 구성해 시스템 안정성을 높였습니다.

### 4. 대용량 연산 최적화를 위한 DB 설계와 확장성 고민
* **의도적 역정규화(Denormalization)를 통한 성능 확보:** 추천 알고리즘 설계상, 서버가 수많은 장비 매물 스펙(공/마, 스탯, 잠재 등)을 메모리로 한 번에 불러와 조합 연산을 수행해야 합니다. 이때 발생하는 무거운 Database JOIN 오버헤드를 원천 차단하기 위해, 장비 테이블을 단일 와이드 테이블(Wide-Column)로 의도적으로 역정규화하여 조회 성능(I/O 속도)을 극대화했습니다.

* **무보엠(무기·보조·엠블렘) 추천 확장을 고려한 스키마 설계:** 현재는 방어구와 장신구 위주의 시뮬레이션이지만, 장비의 가장 핵심 스펙인 공격력 % 및 마력 % 컬럼을 선제적으로 DB 스키마와 엔티티 로직에 촘촘히 설계해 두었습니다. 이를 통해 향후 구조 변경 없이도 무보엠 추천 로직을 즉각 붙일 수 있는 높은 확장성을 열어두었습니다.

* **비즈니스 로직 리팩토링 계획 (OCP 준수):** 현재 서비스 레이어에 하드코딩되어 있는 세트 아이템 분류 로직(contains을 향후 서버 기동 시 인메모리에 올리는 정적 Enum과 Map 해시 구조로 개편할 예정입니다. 새로운 장비 세트가 출시되더라도 기존 핵심 코드를 수정하지 않는 개방-폐쇄 원칙(OCP) 달성을 다음 목표로 잡고 있습니다.
<br>

##  배포 및 실행 방법 (How to run)

AWS EC2 환경에서 **Docker**를 이용해 구동 및 배포합니다.
*(로컬 실행 시 `application.yml`에 Nexon API Key 및 DB 정보 설정이 필요합니다.)*


```bash
# 1. 프로젝트 빌드
./gradlew clean build -x test

# 2. Docker 이미지 빌드
docker build -t maple-recommender .

# 3. 컨테이너 백그라운드 실행 (포트 8080)
docker run -d -p 8080:8080 maple-recommender
